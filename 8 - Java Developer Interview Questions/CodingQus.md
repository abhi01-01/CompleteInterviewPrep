1. Write a program to check if a number is a palindrome without converting it to a string. 
2. Implement a function to sort a stack using another stack. 
3. Write a program to find the maximum sum subarray (Kadane’s algorithm).
4. Write a Java 8 stream code to group employees by department and get the highest salary per department.